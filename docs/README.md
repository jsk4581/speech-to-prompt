# docs/

- [`architecture.md`](architecture.md) — how STP captures voice, transcribes it
  locally, runs the grill loop, and injects exactly one confirmed XML prompt;
  plus the security/privacy model and why the UI is a browser popup.
(The top-level [`README`](../README.md)'s media — `demo.gif`, `popup.png` —
is attached to the [v0.3.0 release](https://github.com/jsk4581/speech-to-prompt/releases/tag/v0.3.0)
rather than checked in, so plugin installs don't carry it.)
